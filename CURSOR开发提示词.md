# Cursor 开发提示词 —— 钢铁行业多租户自动化绩效核算系统

> 使用方式：将本文件作为项目上下文（连同 `docs/` 与 `prototype/` 一并纳入 Cursor 工作区），然后只需说「请开始编码」，即按本提示词的规划与规范执行。**本文件为最高优先级开发依据。**

---

## 0. 你的角色与总目标

你是资深全栈架构师 + 工程师。请构建一套**面向钢铁贸易/加工企业的多租户 SaaS 自动化绩效核算系统**，实现"可配置的取数逻辑 + 可配置的核算规则"，自动完成采购/销售等岗位的绩效工资、提成、计息、奖惩核算，并覆盖规则治理（发布/审批/灰度/审计/版本）与绩效明细出账。

**权威规格来源（务必先完整阅读，作为需求与逻辑的唯一事实来源，不得遗漏）：**
- `docs/01-需求分析与梳理.md` … `docs/07-实施路径与风险.md`：总体需求、架构、采购/销售核算、取数与核算引擎、数据模型、实施路径。
- `docs/08` / `docs/09` / `docs/11`：超扑越、岳洋通、信桥、名亿 四家真实客户方案适配分析。
- `docs/10-通用能力沉淀清单.md`、`docs/12-平台基础能力设计.md`：**平台基础能力（12 项）**——这是系统的核心引擎能力，必须完整实现。
- `prototype/`：全部界面的高保真交互原型（30+ 页），前端 UI/交互/字段/公式以此为准；`prototype/assets/style.css` 为视觉规范参考。

> 若本提示词与 docs 冲突，以能覆盖"全部四家客户 + 平台基础能力"的更完整者为准；有歧义先在计划中列出并采用行业通行默认，不得静默丢弃任何已设计功能。

---

## 1. 技术栈与硬性约束

| 层 | 选型 | 说明 |
| --- | --- | --- |
| 后端 | **Java 17 + Spring Boot 3.x** | Spring Web、Spring Security、Spring Validation、Spring Scheduling、Spring Tx |
| 持久层 | **MyBatis / MyBatis-Plus**（推荐）或 JPA | 平台库建议 MySQL 8 / PostgreSQL；**ERP 源库为 SQL Server 2008 及以上（只读接入）** |
| 前端 | **Vue 3 + Vite + TypeScript** + Pinia + Vue Router | UI 库 Element Plus 或 Ant Design Vue；ECharts 图表 |
| 鉴权 | Spring Security + **JWT**，RBAC + 字段级权限 | 多租户上下文注入 |
| 缓存/调度 | Redis（缓存/分布式锁/幂等）、Quartz 或 Spring Scheduling（跑批/日快照） | |
| 构建 | Maven（多模块）、pnpm/npm | |
| 部署 | Docker + docker-compose（本地一键起） | |

**必须遵守：**
1. **多租户**：所有业务数据、规则、结果均带 `tenant_id`；提供强制租户隔离（见 §5）。
2. **ERP 只读**：连接 SQL Server 的账号/连接串仅授予只读权限；**严禁**对 ERP 写操作、DDL。兼容 SQL Server 2008（注意 OFFSET/FETCH 需 2012+，2008 用 ROW_NUMBER 分页；避免 2012+ 专属函数）。
3. **安全第一**：取数 DSL→SQL 的编译必须白名单化、参数绑定，杜绝 SQL 注入（见 §7）。
4. **可复算 + 可追溯**：任一绩效数字可下钻到原始单据 + 规则版本 + 计算过程；重算幂等。
5. 不得为了跑通而删减已设计功能；如需分期，用里程碑推进（见 §11），但接口/模型要为后续留位。

---

## 2. 总体架构（多模块）

```
apps/
  backend/                     # Spring Boot 多模块
    perf-common               # 通用：多租户上下文、返回体、异常、审计、工具
    perf-security             # 认证授权、RBAC、字段脱敏、审计拦截
    perf-tenant               # 租户管理、租户数据源路由(ERP)
    perf-integration          # ERP/行情/考勤 适配器、ODS→DWD、日快照调度、取数DSL→SQL 编译执行
    perf-metric               # 指标计算(日均库存/应收、周转率、挂价利润、净利润、成本重估、计息)
    perf-engine               # 核算引擎：核算项、公式引擎、阶梯/门槛/阶跃/封顶、系数/达成率映射、成本口径
    perf-rule                 # 规则治理：绩效方案、参数作用域、版本、发布、三级串签审批、灰度、时间轴、审计
    perf-calc                 # 跑批：试算/正式核算、幂等、快照、提成核算单明细
    perf-report               # 报表：驾驶舱、看板、绩效明细、导出、审计查询
    perf-appeal               # 审批流、申诉复核
    perf-api                  # 对外 REST 聚合、装配、启动
  frontend/                    # Vue3 + Vite + TS（对应 prototype 各页面）
db/
  platform/                    # 平台库 DDL + Flyway/Liquibase 迁移
  erp-adapter/                 # SQL Server 只读取数模板/映射
docs/ prototype/               # 既有规格与原型（只读参考）
```

分层：**展现层(Vue) → API/应用服务 → 领域引擎(取数/公式/系数/计息/核算) → 数据集成(ERP适配→ODS→DWD标准中间库) → 存储层**。核算引擎只面向标准中间库，屏蔽 ERP 差异。

---

## 3. 功能模块清单（务必全实现，逐项对应原型页面）

> 每个模块都要有：数据模型 + 服务/接口 + 前端页面 + 单元/集成测试 + 权限控制 + 审计。

### 3.1 数据集成与取数（perf-integration）
- ERP 适配器（SQL Server 2008+ 只读；API/DB 直连/文件导入多形态）、字段映射、品规映射（ERP↔行情）。
- ODS 贴源 → DWD 标准中间库（清洗/维度对齐）；对应 `prototype/data-integration.html`（含字段映射、品规映射可编辑）。
- 行情平台接入（钢银/钢之家）+ 人工导入；对应 `prototype/market-price.html`。
- **每日快照采集调度**：库存/应收/预收/应付预付 每日快照（日均库存/应收、周转率、计息的基础）。
- **取数 DSL → SQL 引擎**（名亿驱动，见 §6、§7）：绩效目标单(指标+起止+条件)+金额/成本公式→按核算对象(员工/部门)编译只读 SQL→写提成核算单明细。对应 `prototype/cap-metric-dsl.html`。

### 3.2 指标计算（perf-metric）
- 日均库存/应收、存货周转率、应收周转率（每日快照聚合）。
- 挂价利润、业务净利润归集；成本重估（移动加权/市场基准/批次，剔除市场自然波动收益）。
- 计息：预收/应收/预付/应付（每日余额×日利率）+ **合同分笔滚动计息 + 总额封顶**（岳洋通期货，`prototype/yyt-interest.html`）。
- 行情价匹配引擎（品名×规格×材质×地区×日期，就近/回退/别名映射）。

### 3.3 核算引擎与平台基础能力（perf-engine）——**系统核心，见 §4**
实现 `docs/12` 的 **12 项平台基础能力**：
1. 核算项引擎（方案=Σ核算项）
2. **多维参数作用域引擎**（业务类型/账期/仓库-库别/材质/品类/供应商/组织-分支/岗位/时间段-淡旺季/客户类型，逐级覆盖取最具体）`prototype/cap-scope.html`
3. 计量口径引擎（出库实重/理算理论重量/内部销售折半）
4. **阶梯/门槛/阶跃/封顶引擎**（整体档位|累进、起步门槛 −10/−20/≥10、每 N 元阶跃 /10、单位封顶 吨利润≤50、目标达成特殊行）`prototype/cap-tier-matrix.html`、`prototype/cap-cost-cap.html`
5. 达成率/完成率映射（→系数 或 →固定档位金额；个人&团队 GROUP BY；跨期引用"上月定下月"）
6. **成本口径引擎**（一票制成本、**结算方式驱动成本** 现款/欠款/垫资、返利与其他收支调整、单据混合归集）`prototype/cap-cost-cap.html`
7. 取数 DSL→SQL（见 3.1/§6）
8. 条件规则与豁免（低价扣减/豁免、欠款不计当月、终端客户账期+10天、备案标记）
9. 计息引擎（见 3.2）
10. 激励专项（新客户开发：按个/按月固定/累计阶段/地区/审核；过磅：加磅提成/点数挂价实时快照；加工费提成）
11. 明细级×期间级关联（逐单提成 与 日均应收/周转率 期间总值 分层运算）
12. 绩效明细 + 规则治理（见下）

### 3.4 采购/销售绩效（perf-engine 规则集）
- 采购：价格竞争力、行情掌控、存货/资金效率、超额奖（能力性收益阶梯×分配×综合系数）、预付计息。`prototype/purchase-board.html`、`simulator-purchase.html`、`sandbox-purchase.html`。
- 销售：现货(阶梯×账期)/零调/直发净利润、挂价利润、应收考核、预收计息、回款账期差异化提成。`prototype/sales-board.html`、`simulator.html`、`sandbox.html`。
- **四家真实客户方案**（作为规则模板/验证基线，务必都能配置跑通）：
  - 超扑越：基本工资(达成率定档,上月定下月)+自营/外配淡旺季阶梯+吨量(组别×客户类型)+加磅+过渡期保底。`prototype/cpy-*.html`
  - 岳洋通：库别销量/差价绩效+加工3%+新户+团队系数+期货滚动计息。`prototype/yyt-*.html`
  - 信桥：账期×业务单价矩阵(含负值)+吨利润封顶+一票制成本+正式/实习岗位。
  - 名亿：吨位阶梯+利润提成(自营/外调)+结算方式成本+目标单转SQL+核算对象。

### 3.5 核算与明细（perf-calc / perf-report）
- 核算工作台：数据就绪校验→指标计算→逐人/团队跑批→异常清单→跑批日志。`prototype/calc-workbench.html`
- 个人绩效单（逐层下钻到公式/指标/ERP原始单据 + 规则版本）。`prototype/payslip.html`
- 提成核算单明细 / 绩效明细表（多项分列 + 按日期筛选不跨月 + 维度筛选）。`prototype/cpy-detail.html`、`yyt-detail.html`
- 试算器/政策沙盘/系数分布回溯对比。`prototype/simulator*.html`、`sandbox*.html`、`rule-config.html`(系数分布)

### 3.6 规则治理（perf-rule）—— `prototype/rule-config.html`
- 绩效方案设计器、账期分档表、系数档表、阶梯提成表、计息参数、公式编辑器、评分卡(含定性折算表 A/B/C/D、一票否决线)、系数分布模拟(权重方案 A/B 回溯对比、采用方案、导出决策报告)。
- **规则变更治理闭环**：发布(权重=100% 等硬校验) → 生成规则变更单 → **三级串签(部门主管→HR负责人 或签→分管领导，支持委托/加签)** → 立即/定时/指定周期生效、**灰度试点** → **灰度效果监测预警** → **推广全量** → **版本时间轴 + 两版本参数 diff** → 绩效单规则版本反查变更单。
- **平台能力统一配置入口**：`rule-config.html` 内嵌 5 项平台能力配置页，能力页改参数→生成变更单→走审批。

### 3.7 审批与申诉（perf-appeal）
- 绩效审批流（主管审核→员工确认→核定归档→发放对接，流转留痕）`prototype/approval.html`。
- 申诉复核（员工申诉→复核下钻比对→裁定重算，闭环留痕）`prototype/appeal.html`。

### 3.8 合规审计（perf-report）—— `prototype/audit.html`
- 全量变更单在线查询（单号/状态/范围筛选、下钻串签留痕与参数快照、导出审计报告）。

### 3.9 报表/驾驶舱/员工端
- 管理驾驶舱 `prototype/dashboard.html`；采购/销售看板；员工端移动绩效 + 消息中心（规则生效通知、可跳转变更单）`prototype/mobile.html`。

### 3.10 系统管理
- 租户管理、组织/岗位/员工、角色权限（字段级/脱敏）、主数据（品规/客户/库别/业务类型/分支机构）、参数字典、审计日志。

---

## 4. 核算引擎设计要点（perf-engine）

- **绩效方案(Plan)** = 适用范围(岗位/部门/人/租户) + 生效期 + 核算项列表 + 系数规则 + 汇总公式 + 版本。
- **核算项(Component)** = 类型(固定/公式/阶梯/系数/分成/计息/扣减/奖励) + 依赖指标 + 作用域 + 公式 + 版本。
- **公式引擎**：支持四则/括号、变量引用(指标/参数/其他核算项)、条件 IF/CASE、聚合 SUM/AVG/日均/SUM_BY_ORDER、查表 LOOKUP/STEP、门槛 THRESHOLD、阶跃 STAIR、封顶 CAP、取整 ROUND/CEIL/FLOOR(含 0.5 特殊进位)。**公式解析用 AST（如自研或 Aviator/MVEL 沙箱），禁止 eval 任意代码**。
- **按依赖拓扑排序计算**；每个公式绑定版本；历史核算按当时版本复算。
- **跑批**：试算/正式；逐人+团队(GROUP BY 部门)；**幂等**（同周期同数据同规则重跑结果一致，用幂等键+快照）；断点续跑；异常标记(缺数/负值/超阈值)。
- **快照不可变**：核算结果保存"数据快照ref + 规则版本ref + 计算明细"，供复算与追溯。

---

## 5. 多租户设计（强制）

- **隔离策略**：平台库采用 **共享库 + `tenant_id` 判别列**；所有表含 `tenant_id`，所有查询强制带租户条件（MyBatis 拦截器/自动条件注入，禁止绕过）。敏感规模客户可选 schema/库隔离，架构需可切换。
- **租户上下文**：登录后解析 JWT 得 `tenant_id + userId + roles`，存 ThreadLocal/请求上下文；服务层与 SQL 层强制使用。跨租户访问一律拒绝并审计。
- **ERP 数据源路由**：每租户配置各自的 SQL Server 只读连接（连接串加密存储），`perf-tenant` 按当前租户路由 ERP 取数。
- **资源隔离**：跑批任务、缓存 key、文件导出均带 `tenant_id` 前缀；防止越权与串数据。
- 单元测试必须包含**跨租户越权访问被拒**的用例。

---

## 6. 取数 DSL → SQL（名亿核心，安全敏感）

- 输入：绩效目标单/核算项的"金额公式、成本公式、使用条件、应用范围、核算对象(员工/部门)"。
- 处理：**将配置编译为参数化只读 SQL**：`SELECT <聚合> FROM dwd_sales_detail WHERE tenant_id=? AND <使用条件(参数绑定)> GROUP BY emp_id|dept_id`。
- 结算方式驱动成本用 `CASE settle_type WHEN ... END`；阶梯用应用层 STEP 或 SQL CASE。
- **明细级×期间级**：逐单提成走明细 SQL；日均应收/周转率等"期间总值"单独查询，作为系数/调整项作用于汇总，**不逐单摊派**（解决名亿提出的关联问题）。
- 结果写入"提成核算单明细"。

---

## 7. 安全要求（达标项，逐条实现并测试）

1. **认证授权**：Spring Security + JWT；密码 BCrypt；登录失败锁定；Token 过期/刷新；接口按角色鉴权。
2. **RBAC + 字段级权限 + 数据脱敏**：薪酬金额按角色可见/脱敏；行级按组织树限定；员工仅见本人。
3. **多租户隔离**：见 §5，强制且不可绕过；越权即拒 + 审计。
4. **SQL 注入防护（重中之重）**：
   - 取数 DSL → SQL 必须：**字段/表名白名单校验**（只允许 DWD 标准视图/字段）、**值一律参数绑定**、禁止拼接用户原始字符串、禁止多语句/注释/DDL/DML(仅 SELECT)、**只读连接执行**、**查询超时 + 返回行数上限**。用 AST/词法校验，拒绝任何不在白名单的标识符。
   - 业务查询统一 MyBatis 预编译参数。
5. **公式引擎沙箱**：禁止反射/IO/网络/任意类调用；仅暴露白名单函数与变量。
6. **ERP 只读**：连接账号只读；代码层禁止非 SELECT。
7. **审计日志**：登录、规则变更/发布/审批、核算、核定、发放、导出、越权尝试 全量留痕（操作人/租户/对象/前后值/时间/IP）。
8. **敏感配置加密**：ERP 连接串、密钥等加密存储（如 Jasypt/KMS）。
9. **传输与依赖**：HTTPS；依赖漏洞扫描；输入校验（Bean Validation）；输出防 XSS（前端转义）；CSRF 策略（JWT 场景说明）。
10. **越权/水平权限**：所有按 id 访问的接口校验归属租户与数据范围。

---

## 8. 稳定性与性能（达标项）

- **跑批幂等 + 可复算**：幂等键(tenant+周期+方案版本+数据快照hash)；重复提交不重复计。
- **事务与一致性**：核算写入用事务；跨步骤用状态机 + 补偿；乐观锁(version)防并发覆盖。
- **快照隔离**：核算依赖当期快照与规则版本，发布新规则不影响已核定结果。
- **分布式锁**：同租户同周期核算串行（Redis 锁）。
- **性能**：单批数千人分钟级完成；日快照采集不影响 ERP 生产（限流/低峰/只读副本优先）；SQL Server 2008 分页用 ROW_NUMBER；大结果集分页/流式；热点参数与规则缓存(带版本失效)。
- **可观测**：结构化日志 + traceId；核算跑批进度与耗时指标；健康检查 `/actuator/health`；关键失败告警。
- **容错**：ERP/行情源不可用时降级(告警+人工补录，不静默跳过)；跑批失败可回滚/断点续跑。

---

## 9. 数据模型（关键表，均含 `tenant_id`、审计字段、软删/version）

- 主数据：`tenant`、`org`、`position`、`employee`(绩效基数)、`product`(品规)、`product_mapping`、`customer`、`biz_type`、`branch`(分支机构)、`store`(库别)。
- ERP/DWD 标准：`dwd_purchase_order`、`dwd_sales_detail`(库别/理算重量/成交价/挂牌价/销售利润2/加工费/结算方式/回款账期/业务类型/内部销售标记/低价备案)、`dwd_inventory_snapshot`、`dwd_receivable_snapshot`、`dwd_advance_receipt_snapshot`、`dwd_payable_snapshot`、`market_price`、`attendance`、`reward_punish`、`futures_contract`+`contract_event`(打款/入库/出库)。
- 指标(DWS)：`metric_result`、`interest_result`。
- 规则：`data_rule`、`plan`、`component`、`coefficient_table`、`tier_table`、`credit_term_table`、`interest_config`、`scorecard`(维度/权重/定性折算/否决线)、`scope_param`(多维作用域参数)、`rule_version`、`change_request`(变更单+串签链+参数快照+生效方式+范围)。
- 结果：`payslip`(绩效单)、`commission_detail`(提成核算单明细)、`workflow`(审批/申诉)、`audit_log`、`notice`(员工消息)。

（以 `docs/06` 数据模型为基础，并按四家客户/平台能力补齐上述字段。）

---

## 10. 前端（frontend，对应 prototype）

- 结构：`views/` 按模块对应原型页；`components/` 复用（KPI卡/表格/图表/时间轴/审批链/下钻树/开关/toast）；`api/` 封装；`store/`(Pinia) 租户与用户态；路由守卫做鉴权。
- 视觉：遵循 `prototype/assets/style.css` 设计规范（配色/圆角/阴影/组件）；图表用 ECharts。
- 交互 1:1 复刻原型：核算工作台、绩效单下钻、规则中心(含平台能力统一入口 iframe/内嵌路由)、审批/申诉、沙盘/回溯/分布、审计查询、员工端移动页(消息中心/申诉)。
- 所有金额/敏感字段按权限脱敏渲染；表单校验；错误提示。

---

## 11. 交付顺序（里程碑；每个里程碑可运行、可演示、含测试）

- **M0 脚手架**：多模块后端 + Vue3 前端 + Docker + 登录鉴权 + 多租户上下文 + DB 迁移 + CI(编译/测试/lint)。
- **M1 数据集成与指标**：ERP(SQL Server)只读适配 + DWD + 日快照调度 + 日均/周转率/挂价利润/净利润指标 + 字段/品规映射页。
- **M2 核算引擎 P0**：核算项引擎、公式引擎(AST沙箱)、**多维参数作用域**、计量口径、**阶梯/门槛/阶跃/封顶**、达成率映射、成本口径 → 能跑通"销售现货 + 采购基础"核算与个人绩效单下钻。
- **M3 规则治理**：绩效方案/参数/版本、发布硬校验、三级串签审批、灰度/推广、版本时间轴/diff、审计查询、平台能力统一配置入口。
- **M4 P1 能力**：取数 DSL→SQL、条件豁免、明细×期间关联、绩效明细报表(不跨月筛选)、评分卡/系数分布回溯、审批流/申诉、员工端消息。
- **M5 四客户落地验证 + P2**：超扑越/岳洋通/信桥/名亿 四套方案纯配置跑通并与其历史 Excel 结果比对一致；计息扩展(期货滚动计息)、过磅(加磅/点数挂价)、加工费提成、新户激励。
- **M6 加固**：安全测试(注入/越权/脱敏)、性能压测、可观测、文档与部署。

> 每完成一个逻辑单元：编写/更新测试 → 自测通过 → 提交（清晰 commit）。里程碑之间保持可运行。

---

## 12. 工作方式与质量门禁（务必遵守）

1. **先出计划再编码**：读完 `docs/` 与 `prototype/` 后，输出总体计划与里程碑任务清单（TODO），再逐项实现；每项完成即勾选。
2. **测试**：核心引擎(公式/阶梯/作用域/成本/计息/达成率)必须单元测试；关键链路集成测试；多租户越权、SQL 注入防护、幂等复算 必须有测试用例。目标核心模块行覆盖率 ≥ 70%。
3. **可运行**：提供 `docker-compose up` 一键起（含平台库、Redis、后端、前端、以及 ERP SQL Server 的连接配置说明/样例数据）。提供 seed 脚本与四客户样例数据用于验证。
4. **不丢功能**：以 §3 模块清单为交付核对表，逐项落实；确无法一次完成的，先建接口/模型占位并在 TODO 标注，严禁悄悄省略。
5. **代码规范**：分层清晰、DTO/VO/Entity 分离、统一返回体与异常、参数校验、日志规范、无魔法值；前端 TS 严格模式。
6. **配置优于硬编码**：绩效规则、参数、系数、阶梯、作用域、计息、成本口径一律可配置 + 版本化，禁止把某客户规则写死在代码里（用规则/参数数据驱动）。
7. **文档**：随代码更新 README、接口文档(OpenAPI/Swagger)、数据库迁移说明、部署说明。

---

## 13. 验收标准（Definition of Done）

- 四家真实客户方案（超扑越/岳洋通/信桥/名亿）均可通过**配置**在系统内跑通，绩效结果与其原始公式/Excel一致，且逐笔可下钻追溯。
- 平台 12 项基础能力全部可视化配置且生效；规则变更走三级串签、灰度、审计、版本可追溯。
- 多租户隔离、字段级权限/脱敏、SQL 注入防护、越权防护、审计留痕 全部通过测试。
- 跑批幂等可复算；性能达标；`docker-compose` 一键运行；核心模块测试通过。

---

**现在，请先阅读 `docs/` 与 `prototype/` 全部内容，输出总体计划与里程碑 TODO，然后从 M0 开始编码。遇到规格歧义时，采用能同时满足四家客户与平台基础能力的通用做法，并在计划中标注假设，不得遗漏任何已设计功能。**
